import { GetColorDirective } from './get-color.directive';

describe('GetColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GetColorDirective();
    expect(directive).toBeTruthy();
  });
});
